[project]
name = "stock"
version = "0.1.0"
description = "Projeto de estudo, a ideia é fazer um sistema de estoque"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.14.14",
]
